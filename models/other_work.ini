[model]
name = Other Work
label = {{ this.title }}

[fields.title]
type = string
label = Title

[fields.short_label]
type = string
label = Short label (optional)

[fields.order]
type = integer
label = Order within parent (optional)

[fields.body]
type = markdown
label = Body
