[model]
name = Postulate
label = {{ this.title }}

[fields.title]
type = string
label = Title

[fields.short_label]
type = string
label = Short label (e.g. "I.Post.1")

[fields.body]
type = html
label = Body
