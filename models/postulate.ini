[model]
name = Postulate
label = {{ this.title }}

[fields.title]
type = string
label = Title

[fields.short_label]
type = string
label = Short label (e.g. "I.Post.1")

[fields.body]
type = html
label = Body

[fields.statement]
type = html
label = One-line statement / claim

[fields.is_construction]
type = boolean
label = Is a construction (Q.E.F.)?

[fields.order]
type = integer
label = Sort order within section
