[model]
name = Book
label = {{ this.title }}

[fields.title]
type = string
label = Title

# Optional intro prose; the book template auto-renders a TOC of
# children (definitions / postulates / common notions / propositions)
# below this body.
[fields.body]
type = html
label = Intro body
