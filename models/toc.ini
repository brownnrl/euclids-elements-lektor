[model]
name = Table of Contents
label = {{ this.title }}

[fields.title]
type = string
label = Title

# Optional intro prose above the auto-generated book list.
[fields.body]
type = html
label = Intro body

[children]
model = book
order_by = title
