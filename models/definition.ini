[model]
name = Definition
label = {{ this.title }}

[fields.title]
type = string
label = Title

[fields.short_label]
type = string
label = Short label (e.g. "I.Def.1")
description = Shown next to the link in parent book TOC.

[fields.body]
type = html
label = Body

[fields.statement]
type = html
label = One-line statement / claim

[fields.order]
type = integer
label = Sort order within section

[fields.red_highlight]
type = boolean
label = Render in red (highlight)?
