[model]
name = Common Notion
label = {{ this.title }}

[fields.title]
type = string
label = Title

[fields.short_label]
type = string
label = Short label (e.g. "C.N.")

[fields.body]
type = html
label = Body

[fields.statement]
type = html
label = One-line statement / claim

[fields.order]
type = integer
label = Sort order within section

[fields.red_highlight]
type = boolean
label = Render in red (highlight)?
